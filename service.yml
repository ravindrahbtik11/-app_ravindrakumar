apiVersion: v1
kind: Service
metadata:
  labels:
    app: nagp-dotnet-dev
  name: nagp-dotnet-dev
  namespace: kubernetes-cluster-ravindrakumar
spec:
  ports:
  - nodePort: 31110
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: nagp-dotnet-dev
    type: develop
  type: LoadBalancer